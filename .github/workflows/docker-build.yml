name: Docker Build

on:
  push:
    branches: [main]
    paths: ["Backend/**"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t ai-dashboard-backend -f Backend/Dockerfile Backend
